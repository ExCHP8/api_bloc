name: api_bloc
description: A Flutter library for managing API calls using the BLoC pattern.
  This library provides a set of classes and utilities to simplify API calls and
  manage state changes.
version: 1.7.0
homepage: https://github.com/Nialixus/api_bloc.git
repository: https://github.com/Nialixus/api_bloc.git
issue_tracker: https://github.com/Nialixus/api_bloc/issues
funding: https://www.buymeacoffee.com/nialixus
topics:
  - bloc
  - api

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  args: ^2.4.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
flutter: null